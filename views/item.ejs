{
    "name": 
                <% if(data.length){ 
                                
                for(var i = 0;i < data.length;i++)
                { %> %=data[i].name%> <% }
            
                    } else { %>

                <% }
                %>
}