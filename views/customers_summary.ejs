{
    <p id="demo"></p>
    "<%=page_item%>":                              
            <% if(data.length){           
                for(var i = 0;i < data.length;i++) { %>
                    "company":"<%=data[i].company%>",
                    "name":"<%=data[i].name%>"

                <% 
document.getElementById("demo").innerHTML = data.length;
                if (i < data.length) { %>
                        a
                    <% } else { %>
                        b
                    <% }
 
                }
             } else { %>
                <% } %>
}