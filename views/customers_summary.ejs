{
    "<%=page_item%>":                              
            <% if(data.length){           
                for(var i = 0;i < data.length;i++) { 
 
                if (i > 0) { %>
                        }, {
                    <% } else { %>
                        none
                    <% }
 
                  %>  "company":"<%=data[i].company%>",
                    "name":"<%=data[i].name%>"

                <% 
                }
             } else { %>
                <% } %>
}